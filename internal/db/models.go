// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0

package db

import (
	"encoding/json"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

// Lịch sử phê duyệt — append-only, không sửa/xóa. Phục vụ workflow engine
type ApprovalAction struct {
	ID uuid.UUID `json:"id"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO
	DealModule string    `json:"deal_module"`
	DealID     uuid.UUID `json:"deal_id"`
	// Loại hành động: DESK_HEAD_APPROVE, DIRECTOR_REJECT, DEALER_RECALL...
	ActionType string `json:"action_type"`
	// Trạng thái trước hành động
	StatusBefore string `json:"status_before"`
	// Trạng thái sau hành động
	StatusAfter string    `json:"status_after"`
	PerformedBy uuid.UUID `json:"performed_by"`
	PerformedAt time.Time `json:"performed_at"`
	// Lý do — bắt buộc cho reject, recall, cancel
	Reason *string `json:"reason"`
	// Dữ liệu bổ sung (vd: snapshot hạn mức)
	Metadata []byte `json:"metadata"`
}

// Nhật ký kiểm toán — APPEND-ONLY, không sửa/xóa. Phân vùng theo tháng (BRD section 8)
type AuditLog struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202601 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202602 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202603 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202604 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202605 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202606 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202607 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202608 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202609 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202610 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202611 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogs202612 struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

type AuditLogsDefault struct {
	ID uuid.UUID `json:"id"`
	// ID người thực hiện — FK logic (không enforce để tránh ảnh hưởng partition)
	UserID uuid.UUID `json:"user_id"`
	// Tên đầy đủ — snapshot tránh JOIN (dữ liệu kiểm toán phải tự đầy đủ)
	UserFullName string `json:"user_full_name"`
	// Phòng ban — snapshot
	UserDepartment *string `json:"user_department"`
	// Mã chi nhánh — snapshot
	UserBranchCode *string `json:"user_branch_code"`
	// 14 loại sự kiện theo BRD 8.1: CREATE, EDIT, APPROVE, REJECT, RECALL, CANCEL_REQUEST, CANCEL_APPROVE, CANCEL_REJECT, BOOK, SETTLE, LOGIN, LOGOUT, EXPORT, SYSTEM
	Action string `json:"action"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO, CREDIT_LIMIT, INTERNATIONAL_PAYMENT, SYSTEM
	DealModule   string      `json:"deal_module"`
	DealID       pgtype.UUID `json:"deal_id"`
	StatusBefore *string     `json:"status_before"`
	StatusAfter  *string     `json:"status_after"`
	// Giá trị cũ (khi sửa) — JSONB diff
	OldValues []byte `json:"old_values"`
	// Giá trị mới (khi sửa) — JSONB diff
	NewValues []byte `json:"new_values"`
	// Lý do — cho reject, recall, cancel
	Reason *string `json:"reason"`
	// IP client
	IpAddress string  `json:"ip_address"`
	UserAgent *string `json:"user_agent"`
	// Thời điểm chính xác — partition key
	PerformedAt time.Time `json:"performed_at"`
}

// Cấu hình xác thực — chuyển đổi standalone ↔ Zitadel không cần migration
type AuthConfig struct {
	ID uuid.UUID `json:"id"`
	// Chế độ: standalone (tự quản lý) hoặc zitadel (OIDC)
	AuthMode  string  `json:"auth_mode"`
	IssuerUrl *string `json:"issuer_url"`
	ClientID  *string `json:"client_id"`
	// Client secret đã mã hóa — KHÔNG lưu plain text
	ClientSecretEncrypted *string     `json:"client_secret_encrypted"`
	Scopes                *string     `json:"scopes"`
	AutoCreateUser        bool        `json:"auto_create_user"`
	SyncUserInfo          bool        `json:"sync_user_info"`
	IsActive              bool        `json:"is_active"`
	UpdatedAt             time.Time   `json:"updated_at"`
	UpdatedBy             pgtype.UUID `json:"updated_by"`
}

// Danh mục trái phiếu / giấy tờ có giá — TPCP, TCTC, GTCG
type BondCatalog struct {
	ID uuid.UUID `json:"id"`
	// Mã trái phiếu: TD2135068
	BondCode string `json:"bond_code"`
	Issuer   string `json:"issuer"`
	// Lãi suất coupon (%/năm)
	CouponRate pgtype.Numeric `json:"coupon_rate"`
	// Kỳ trả coupon: ANNUAL, SEMI_ANNUAL, QUARTERLY, ZERO_COUPON (Phase 2)
	PaymentFrequency *string     `json:"payment_frequency"`
	IssueDate        pgtype.Date `json:"issue_date"`
	MaturityDate     pgtype.Date `json:"maturity_date"`
	// Mệnh giá (VND) — số nguyên
	FaceValue pgtype.Numeric `json:"face_value"`
	// Loại: GOVERNMENT (TPCP), FINANCIAL_INSTITUTION (TCTC), CERTIFICATE_OF_DEPOSIT (GTCG)
	BondType  string      `json:"bond_type"`
	IsActive  bool        `json:"is_active"`
	CreatedAt time.Time   `json:"created_at"`
	CreatedBy pgtype.UUID `json:"created_by"`
	UpdatedAt time.Time   `json:"updated_at"`
	UpdatedBy pgtype.UUID `json:"updated_by"`
}

// Giao dịch trái phiếu / GTCG — TPCP, TCTC, GTCG
type BondDeal struct {
	ID uuid.UUID `json:"id"`
	// Mã giao dịch gapless: G-20260403-0001 (Govi), F-20260403-0001 (FI/CD)
	DealNumber string `json:"deal_number"`
	// Phân loại: GOVERNMENT (TPCP), FINANCIAL_INSTITUTION (TCTC), CERTIFICATE_OF_DEPOSIT (GTCG)
	BondCategory string      `json:"bond_category"`
	TradeDate    pgtype.Date `json:"trade_date"`
	BranchID     uuid.UUID   `json:"branch_id"`
	OrderDate    pgtype.Date `json:"order_date"`
	ValueDate    pgtype.Date `json:"value_date"`
	// Hướng: BUY (mua), SELL (bán)
	Direction      string    `json:"direction"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	// Loại giao dịch: REPO, REVERSE_REPO, OUTRIGHT, OTHER
	TransactionType      string         `json:"transaction_type"`
	TransactionTypeOther *string        `json:"transaction_type_other"`
	BondCatalogID        pgtype.UUID    `json:"bond_catalog_id"`
	BondCodeManual       *string        `json:"bond_code_manual"`
	Issuer               string         `json:"issuer"`
	CouponRate           pgtype.Numeric `json:"coupon_rate"`
	IssueDate            pgtype.Date    `json:"issue_date"`
	MaturityDate         pgtype.Date    `json:"maturity_date"`
	// Số lượng trái phiếu (integer)
	Quantity int64 `json:"quantity"`
	// Mệnh giá VND (NUMERIC(20,0))
	FaceValue    pgtype.Numeric `json:"face_value"`
	DiscountRate pgtype.Numeric `json:"discount_rate"`
	// Giá sạch VND
	CleanPrice pgtype.Numeric `json:"clean_price"`
	// Giá thanh toán/dirty price VND
	SettlementPrice pgtype.Numeric `json:"settlement_price"`
	// Tổng giá trị = quantity × settlement_price
	TotalValue pgtype.Numeric `json:"total_value"`
	// Danh mục đầu tư: HTM, AFS, HFT — chỉ khi BUY
	PortfolioType *string     `json:"portfolio_type"`
	PaymentDate   pgtype.Date `json:"payment_date"`
	// Thời hạn còn lại (ngày) = maturity_date − payment_date
	RemainingTenorDays int32              `json:"remaining_tenor_days"`
	ConfirmationMethod string             `json:"confirmation_method"`
	ConfirmationOther  *string            `json:"confirmation_other"`
	ContractPreparedBy string             `json:"contract_prepared_by"`
	Status             string             `json:"status"`
	Note               *string            `json:"note"`
	ClonedFromID       pgtype.UUID        `json:"cloned_from_id"`
	CancelReason       *string            `json:"cancel_reason"`
	CancelRequestedBy  pgtype.UUID        `json:"cancel_requested_by"`
	CancelRequestedAt  pgtype.Timestamptz `json:"cancel_requested_at"`
	CreatedAt          time.Time          `json:"created_at"`
	CreatedBy          uuid.UUID          `json:"created_by"`
	UpdatedAt          time.Time          `json:"updated_at"`
	UpdatedBy          uuid.UUID          `json:"updated_by"`
	DeletedAt          pgtype.Timestamptz `json:"deleted_at"`
}

// Tồn kho trái phiếu — kiểm tra số lượng khả dụng khi bán
type BondInventory struct {
	ID            uuid.UUID   `json:"id"`
	BondCatalogID pgtype.UUID `json:"bond_catalog_id"`
	BondCode      string      `json:"bond_code"`
	BondCategory  string      `json:"bond_category"`
	PortfolioType string      `json:"portfolio_type"`
	// Số lượng khả dụng — hard block khi bán vượt tồn kho
	AvailableQuantity int64 `json:"available_quantity"`
	// Ngày mua ban đầu
	AcquisitionDate pgtype.Date `json:"acquisition_date"`
	// Giá mua ban đầu (VND)
	AcquisitionPrice pgtype.Numeric `json:"acquisition_price"`
	// Phiên bản — optimistic locking ngăn oversell
	Version   int32       `json:"version"`
	UpdatedAt time.Time   `json:"updated_at"`
	UpdatedBy pgtype.UUID `json:"updated_by"`
}

// Chi nhánh / Phòng giao dịch KienlongBank — Phase 1 chỉ có HO, sẵn sàng mở rộng
type Branch struct {
	ID uuid.UUID `json:"id"`
	// Mã chi nhánh: HO (Hội sở), HCM01, HN01, KG01...
	Code string `json:"code"`
	Name string `json:"name"`
	// Loại: HEAD_OFFICE, BRANCH, SUB_BRANCH, TRANSACTION_OFFICE
	BranchType string `json:"branch_type"`
	// Chi nhánh cha — hỗ trợ cấu trúc phân cấp
	ParentBranchID pgtype.UUID `json:"parent_branch_id"`
	// Mã chi nhánh Flexcube Core Banking — Phase 2 tích hợp
	FlexcubeBranchCode *string `json:"flexcube_branch_code"`
	// Mã chi nhánh SWIFT (nếu có)
	SwiftBranchCode *string     `json:"swift_branch_code"`
	Address         *string     `json:"address"`
	IsActive        bool        `json:"is_active"`
	CreatedAt       time.Time   `json:"created_at"`
	CreatedBy       pgtype.UUID `json:"created_by"`
	UpdatedAt       time.Time   `json:"updated_at"`
	UpdatedBy       pgtype.UUID `json:"updated_by"`
}

// Lịch ngày làm việc — dùng cho T+1/T+2 settlement date và tra cứu tỷ giá
type BusinessCalendar struct {
	ID uuid.UUID `json:"id"`
	// Ngày trong năm
	CalendarDate pgtype.Date `json:"calendar_date"`
	CountryCode  string      `json:"country_code"`
	// true = ngày làm việc, false = nghỉ/lễ
	IsBusinessDay bool `json:"is_business_day"`
	// Tên ngày nghỉ/lễ: Tết Nguyên đán, 30/4, 2/9...
	HolidayName *string   `json:"holiday_name"`
	CreatedAt   time.Time `json:"created_at"`
}

// Đối tác giao dịch — ngân hàng, tổ chức tài chính, KBNN, SBV
type Counterparty struct {
	ID uuid.UUID `json:"id"`
	// Mã nội bộ đối tác: MSBI, ACB, VCB...
	Code      string  `json:"code"`
	FullName  string  `json:"full_name"`
	ShortName *string `json:"short_name"`
	// Mã khách hàng CIF từ Core Banking
	Cif string `json:"cif"`
	// SWIFT/BIC code — không bắt buộc với mọi đối tác
	SwiftCode   *string `json:"swift_code"`
	CountryCode *string `json:"country_code"`
	TaxID       *string `json:"tax_id"`
	Address     *string `json:"address"`
	// FX deals có tiêu thụ credit limit không (v3 feature)
	FxUsesLimit bool        `json:"fx_uses_limit"`
	IsActive    bool        `json:"is_active"`
	CreatedAt   time.Time   `json:"created_at"`
	CreatedBy   pgtype.UUID `json:"created_by"`
	UpdatedAt   time.Time   `json:"updated_at"`
	UpdatedBy   pgtype.UUID `json:"updated_by"`
	// Soft delete — đối tác đã xóa vẫn giữ để tra cứu lịch sử
	DeletedAt pgtype.Timestamptz `json:"deleted_at"`
}

// Hạn mức tín dụng — SCD Type 2: lưu lịch sử thay đổi, is_current = true cho bản ghi hiện tại
type CreditLimit struct {
	ID             uuid.UUID `json:"id"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	// Loại: COLLATERALIZED (có TSBĐ) hoặc UNCOLLATERALIZED (không có TSBĐ)
	LimitType string `json:"limit_type"`
	// Hạn mức VND — NULL khi is_unlimited = true
	LimitAmount pgtype.Numeric `json:"limit_amount"`
	// Hạn mức không giới hạn (theo phê duyệt TGĐ)
	IsUnlimited bool `json:"is_unlimited"`
	// Ngày bắt đầu hiệu lực phiên bản này
	EffectiveFrom pgtype.Date `json:"effective_from"`
	// Ngày kết thúc hiệu lực — NULL cho phiên bản hiện tại
	EffectiveTo pgtype.Date `json:"effective_to"`
	// Bản ghi hiện tại — false cho các phiên bản cũ
	IsCurrent  bool        `json:"is_current"`
	ExpiryDate pgtype.Date `json:"expiry_date"`
	// Tham chiếu phê duyệt TGĐ/HĐQT
	ApprovalReference *string     `json:"approval_reference"`
	CreatedAt         time.Time   `json:"created_at"`
	CreatedBy         pgtype.UUID `json:"created_by"`
	UpdatedAt         time.Time   `json:"updated_at"`
	UpdatedBy         pgtype.UUID `json:"updated_by"`
}

// Danh mục tiền tệ theo ISO 4217
type Currency struct {
	ID uuid.UUID `json:"id"`
	// Mã tiền tệ ISO 4217: USD, VND, EUR, AUD, GBP, JPY, KRW...
	Code string `json:"code"`
	// Mã số ISO 4217: USD=840, VND=704
	NumericCode *int16 `json:"numeric_code"`
	Name        string `json:"name"`
	// Số chữ số thập phân: VND=0, USD=2, JPY=0
	DecimalPlaces int16 `json:"decimal_places"`
	IsActive      bool  `json:"is_active"`
}

// Cặp tiền tệ — quyết định công thức tính FX (MULTIPLY/DIVIDE)
type CurrencyPair struct {
	ID            uuid.UUID `json:"id"`
	BaseCurrency  string    `json:"base_currency"`
	QuoteCurrency string    `json:"quote_currency"`
	// Mã cặp: USD/VND, EUR/USD, EUR/GBP
	PairCode string `json:"pair_code"`
	// Độ chính xác tỷ giá: 2 cho USD/VND, 4 cho EUR/USD
	RateDecimalPlaces int16 `json:"rate_decimal_places"`
	// Quy tắc tính: MULTIPLY (USD/VND) hoặc DIVIDE (USD/JPY)
	CalculationRule string `json:"calculation_rule"`
	// Tiền tệ kết quả sau tính toán
	ResultCurrency string `json:"result_currency"`
	IsActive       bool   `json:"is_active"`
}

// Sinh mã giao dịch gapless — advisory lock per module+date
type DealSequence struct {
	ID uuid.UUID `json:"id"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO
	Module string `json:"module"`
	// Tiền tố: FX, G, F, MM, OMO, RK
	Prefix string `json:"prefix"`
	// Ngày làm việc — reset sequence mỗi ngày
	DatePartition pgtype.Date `json:"date_partition"`
	// Số thứ tự cuối cùng — SELECT FOR UPDATE để tăng
	LastSequence int64     `json:"last_sequence"`
	UpdatedAt    time.Time `json:"updated_at"`
}

// Tài liệu đính kèm — lưu trữ MinIO/S3, bảng này chỉ chứa metadata
type Document struct {
	ID uuid.UUID `json:"id"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO
	DealModule string    `json:"deal_module"`
	DealID     uuid.UUID `json:"deal_id"`
	// Loại: TICKET, CONTRACT, CONFIRMATION, SUPPORTING, OTHER
	DocumentType string `json:"document_type"`
	FileName     string `json:"file_name"`
	// Tên bucket S3/MinIO: treasury-docs
	StorageBucket string `json:"storage_bucket"`
	// S3 object key: {module}/{deal_id}/{uuid}_{filename}
	StorageKey string `json:"storage_key"`
	FileSize   int64  `json:"file_size"`
	MimeType   string `json:"mime_type"`
	// SHA-256 hash — kiểm tra tính toàn vẹn
	ChecksumSha256 string `json:"checksum_sha256"`
	// Trạng thái quét virus: PENDING, CLEAN, INFECTED, SKIPPED
	ScanStatus string             `json:"scan_status"`
	ScannedAt  pgtype.Timestamptz `json:"scanned_at"`
	// Phiên bản tài liệu — overwrite tạo version mới
	Version int32 `json:"version"`
	// Phiên bản mới nhất — false cho các phiên bản cũ
	IsCurrent  bool               `json:"is_current"`
	UploadedBy uuid.UUID          `json:"uploaded_by"`
	UploadedAt time.Time          `json:"uploaded_at"`
	DeletedAt  pgtype.Timestamptz `json:"deleted_at"`
}

// Tỷ giá hối đoái — dùng cho quy đổi credit limit, công bố cuối ngày
type ExchangeRate struct {
	ID           uuid.UUID `json:"id"`
	CurrencyCode string    `json:"currency_code"`
	// Ngày hiệu lực (cuối ngày làm việc trước đó)
	EffectiveDate    pgtype.Date    `json:"effective_date"`
	BuyTransferRate  pgtype.Numeric `json:"buy_transfer_rate"`
	SellTransferRate pgtype.Numeric `json:"sell_transfer_rate"`
	// Tỷ giá trung bình = (buy + sell) / 2 — tính sẵn
	MidRate pgtype.Numeric `json:"mid_rate"`
	// Nguồn tỷ giá: KLB_DAILY, SBV
	Source    string      `json:"source"`
	CreatedAt time.Time   `json:"created_at"`
	CreatedBy pgtype.UUID `json:"created_by"`
}

// Ánh xạ nhóm/vai trò từ Zitadel IdP sang vai trò nội bộ Treasury
type ExternalRoleMapping struct {
	ID uuid.UUID `json:"id"`
	// Tên group/role trong Zitadel
	ExternalGroup string    `json:"external_group"`
	RoleID        uuid.UUID `json:"role_id"`
	CreatedAt     time.Time `json:"created_at"`
}

// Giao dịch ngoại hối — header. Spot/Forward = 1 leg, Swap = 2 legs
type FxDeal struct {
	ID uuid.UUID `json:"id"`
	// Mã giao dịch gapless: FX-20260403-0001 — sinh từ deal_sequences
	DealNumber     string    `json:"deal_number"`
	TicketNumber   *string   `json:"ticket_number"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	// Loại: SPOT, FORWARD, SWAP
	DealType string `json:"deal_type"`
	// Hướng: SELL, BUY (Spot/Fwd); SELL_BUY, BUY_SELL (Swap)
	Direction string `json:"direction"`
	// Số tiền giao dịch (FCY, 2 chữ số thập phân)
	NotionalAmount pgtype.Numeric `json:"notional_amount"`
	CurrencyCode   string         `json:"currency_code"`
	// Cặp tiền tệ: USD/VND, EUR/USD...
	PairCode  string      `json:"pair_code"`
	TradeDate pgtype.Date `json:"trade_date"`
	BranchID  uuid.UUID   `json:"branch_id"`
	// Giao dịch có tiêu thụ credit limit không
	UsesCreditLimit bool `json:"uses_credit_limit"`
	// Trạng thái: OPEN → ... → COMPLETED / CANCELLED
	Status string  `json:"status"`
	Note   *string `json:"note"`
	// ID giao dịch gốc khi clone
	ClonedFromID      pgtype.UUID        `json:"cloned_from_id"`
	CancelReason      *string            `json:"cancel_reason"`
	CancelRequestedBy pgtype.UUID        `json:"cancel_requested_by"`
	CancelRequestedAt pgtype.Timestamptz `json:"cancel_requested_at"`
	CreatedAt         time.Time          `json:"created_at"`
	CreatedBy         uuid.UUID          `json:"created_by"`
	UpdatedAt         time.Time          `json:"updated_at"`
	UpdatedBy         uuid.UUID          `json:"updated_by"`
	DeletedAt         pgtype.Timestamptz `json:"deleted_at"`
}

// Legs giao dịch FX — Spot/Forward = 1 leg, Swap = 2 legs (near + far)
type FxDealLeg struct {
	ID     uuid.UUID `json:"id"`
	DealID uuid.UUID `json:"deal_id"`
	// 1 = near leg (Spot/Forward/Swap), 2 = far leg (Swap only)
	LegNumber      int16       `json:"leg_number"`
	ValueDate      pgtype.Date `json:"value_date"`
	SettlementDate pgtype.Date `json:"settlement_date"`
	// Tỷ giá giao dịch (6 chữ số thập phân)
	ExchangeRate pgtype.Numeric `json:"exchange_rate"`
	// Số tiền quy đổi = notional × rate hoặc notional / rate
	ConvertedAmount   pgtype.Numeric `json:"converted_amount"`
	ConvertedCurrency string         `json:"converted_currency"`
	// Pay code KLB
	InternalSsiID uuid.UUID `json:"internal_ssi_id"`
	// Pay code đối tác
	CounterpartySsiID uuid.UUID `json:"counterparty_ssi_id"`
	// Cần thanh toán quốc tế (tạo bản ghi international_payments)
	RequiresInternationalSettlement bool        `json:"requires_international_settlement"`
	CreatedAt                       time.Time   `json:"created_at"`
	UpdatedAt                       time.Time   `json:"updated_at"`
	UpdatedBy                       pgtype.UUID `json:"updated_by"`
}

// Thanh toán quốc tế — tự động tạo khi deal chuyển sang Pending Settlement (BRD 3.5)
type InternationalPayment struct {
	ID uuid.UUID `json:"id"`
	// Module nguồn: FX hoặc MM
	SourceModule string `json:"source_module"`
	// ID deal nguồn
	SourceDealID uuid.UUID `json:"source_deal_id"`
	// Swap: 1 (near) hoặc 2 (far); deal khác: NULL
	SourceLegNumber *int16 `json:"source_leg_number"`
	// Mã hiển thị — Swap có suffix A/B cho 2 legs
	TicketDisplay  string    `json:"ticket_display"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	// Tài khoản nostro KLB (HABIB)
	DebitAccount string `json:"debit_account"`
	// BIC code đối tác
	BicCode      *string `json:"bic_code"`
	CurrencyCode string  `json:"currency_code"`
	// Số tiền chuyển (FCY)
	Amount pgtype.Numeric `json:"amount"`
	// Ngày thanh toán = settlement date
	TransferDate pgtype.Date `json:"transfer_date"`
	// Thông tin SSI đối tác (snapshot text)
	CounterpartySsi    string      `json:"counterparty_ssi"`
	OriginalTradeDate  pgtype.Date `json:"original_trade_date"`
	ApprovedByDivision *string     `json:"approved_by_division"`
	// Trạng thái: PENDING → APPROVED / REJECTED
	SettlementStatus string             `json:"settlement_status"`
	SettledBy        pgtype.UUID        `json:"settled_by"`
	SettledAt        pgtype.Timestamptz `json:"settled_at"`
	RejectionReason  *string            `json:"rejection_reason"`
	CreatedAt        time.Time          `json:"created_at"`
}

// Phê duyệt hạn mức per-deal — snapshot tại thời điểm duyệt (BRD 8.3)
type LimitApprovalRecord struct {
	ID uuid.UUID `json:"id"`
	// Module nguồn: FX hoặc MM
	DealModule string `json:"deal_module"`
	// ID deal nguồn (fx_deals.id hoặc mm_interbank_deals.id)
	DealID         uuid.UUID `json:"deal_id"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	LimitType      string    `json:"limit_type"`
	// Giá trị deal quy VND tại thời điểm duyệt
	DealAmountVnd pgtype.Numeric `json:"deal_amount_vnd"`
	// Snapshot hạn mức: granted, utilized, remaining tại thời điểm duyệt
	LimitSnapshot         json.RawMessage    `json:"limit_snapshot"`
	RiskOfficerApprovedBy pgtype.UUID        `json:"risk_officer_approved_by"`
	RiskOfficerApprovedAt pgtype.Timestamptz `json:"risk_officer_approved_at"`
	RiskHeadApprovedBy    pgtype.UUID        `json:"risk_head_approved_by"`
	RiskHeadApprovedAt    pgtype.Timestamptz `json:"risk_head_approved_at"`
	// Trạng thái: PENDING, APPROVED, REJECTED
	ApprovalStatus  string    `json:"approval_status"`
	RejectionReason *string   `json:"rejection_reason"`
	CreatedAt       time.Time `json:"created_at"`
}

// Snapshot sử dụng hạn mức — append-only, không sửa đổi
type LimitUtilizationSnapshot struct {
	ID             uuid.UUID `json:"id"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	// Ngày chụp snapshot
	SnapshotDate pgtype.Date `json:"snapshot_date"`
	LimitType    string      `json:"limit_type"`
	// Hạn mức cấp — NULL = unlimited
	LimitGranted pgtype.Numeric `json:"limit_granted"`
	// Sử dụng đầu ngày (VND quy đổi)
	UtilizedOpening pgtype.Numeric `json:"utilized_opening"`
	// Sử dụng trong ngày (VND quy đổi)
	UtilizedIntraday pgtype.Numeric `json:"utilized_intraday"`
	// Tổng sử dụng = opening + intraday
	UtilizedTotal pgtype.Numeric `json:"utilized_total"`
	// Còn lại — NULL = unlimited
	Remaining pgtype.Numeric `json:"remaining"`
	// Tỷ giá USD→VND mid rate dùng quy đổi
	FxRateApplied pgtype.Numeric `json:"fx_rate_applied"`
	// Chi tiết phân tách: MM deals + FX deals + FI Bond settlement prices
	BreakdownDetail []byte      `json:"breakdown_detail"`
	CreatedAt       time.Time   `json:"created_at"`
	CreatedBy       pgtype.UUID `json:"created_by"`
}

// Giao dịch liên ngân hàng — gửi/nhận tiền gửi, cho vay/đi vay
type MmInterbankDeal struct {
	ID uuid.UUID `json:"id"`
	// Mã giao dịch gapless: MM-20260403-0001
	DealNumber        string    `json:"deal_number"`
	TicketNumber      *string   `json:"ticket_number"`
	CounterpartyID    uuid.UUID `json:"counterparty_id"`
	BranchID          uuid.UUID `json:"branch_id"`
	CurrencyCode      string    `json:"currency_code"`
	InternalSsiID     uuid.UUID `json:"internal_ssi_id"`
	CounterpartySsiID uuid.UUID `json:"counterparty_ssi_id"`
	// Hướng: PLACE (Gửi TG), TAKE (Nhận TG), LEND (Cho vay), BORROW (Đi vay)
	Direction string `json:"direction"`
	// Số tiền gốc (FCY: 2 chữ số)
	PrincipalAmount pgtype.Numeric `json:"principal_amount"`
	// Lãi suất (%/năm, 6 chữ số thập phân)
	InterestRate pgtype.Numeric `json:"interest_rate"`
	// Quy ước tính ngày: ACT_365, ACT_360, ACT_ACT
	DayCountConvention string      `json:"day_count_convention"`
	TradeDate          pgtype.Date `json:"trade_date"`
	EffectiveDate      pgtype.Date `json:"effective_date"`
	TenorDays          int32       `json:"tenor_days"`
	MaturityDate       pgtype.Date `json:"maturity_date"`
	// Tiền lãi = principal × rate × tenor / day_basis
	InterestAmount pgtype.Numeric `json:"interest_amount"`
	// Số tiền đáo hạn = principal + interest
	MaturityAmount pgtype.Numeric `json:"maturity_amount"`
	// Có tài sản đảm bảo không — ảnh hưởng tính credit limit
	HasCollateral         bool    `json:"has_collateral"`
	CollateralCurrency    *string `json:"collateral_currency"`
	CollateralDescription *string `json:"collateral_description"`
	// Cần thanh toán quốc tế (tạo bản ghi international_payments)
	RequiresInternationalSettlement bool `json:"requires_international_settlement"`
	// Trạng thái — bao gồm PENDING_RISK_APPROVAL cho QLRR
	Status            string             `json:"status"`
	Note              *string            `json:"note"`
	ClonedFromID      pgtype.UUID        `json:"cloned_from_id"`
	CancelReason      *string            `json:"cancel_reason"`
	CancelRequestedBy pgtype.UUID        `json:"cancel_requested_by"`
	CancelRequestedAt pgtype.Timestamptz `json:"cancel_requested_at"`
	CreatedAt         time.Time          `json:"created_at"`
	CreatedBy         uuid.UUID          `json:"created_by"`
	UpdatedAt         time.Time          `json:"updated_at"`
	UpdatedBy         uuid.UUID          `json:"updated_by"`
	DeletedAt         pgtype.Timestamptz `json:"deleted_at"`
}

// Giao dịch OMO (Ngân hàng Nhà nước) và Repo Kho bạc Nhà nước
type MmOmoRepoDeal struct {
	ID         uuid.UUID `json:"id"`
	DealNumber string    `json:"deal_number"`
	// Loại: OMO (với NHNN) hoặc STATE_REPO (với KBNN)
	DealSubtype string `json:"deal_subtype"`
	// Tên phiên giao dịch: Session 1, Session 2...
	SessionName    string      `json:"session_name"`
	TradeDate      pgtype.Date `json:"trade_date"`
	BranchID       uuid.UUID   `json:"branch_id"`
	CounterpartyID uuid.UUID   `json:"counterparty_id"`
	// Mệnh giá giao dịch VND (NUMERIC(20,0))
	NotionalAmount pgtype.Numeric `json:"notional_amount"`
	BondCatalogID  uuid.UUID      `json:"bond_catalog_id"`
	// Lãi suất trúng thầu (%/năm)
	WinningRate pgtype.Numeric `json:"winning_rate"`
	TenorDays   int32          `json:"tenor_days"`
	// Ngày thanh toán lần 1 (mua/bán)
	SettlementDate1 pgtype.Date `json:"settlement_date_1"`
	// Ngày thanh toán lần 2 (mua lại/bán lại)
	SettlementDate2 pgtype.Date `json:"settlement_date_2"`
	// Tỷ lệ chiết khấu (%)
	HaircutPct        pgtype.Numeric     `json:"haircut_pct"`
	Status            string             `json:"status"`
	Note              *string            `json:"note"`
	ClonedFromID      pgtype.UUID        `json:"cloned_from_id"`
	CancelReason      *string            `json:"cancel_reason"`
	CancelRequestedBy pgtype.UUID        `json:"cancel_requested_by"`
	CancelRequestedAt pgtype.Timestamptz `json:"cancel_requested_at"`
	CreatedAt         time.Time          `json:"created_at"`
	CreatedBy         uuid.UUID          `json:"created_by"`
	UpdatedAt         time.Time          `json:"updated_at"`
	UpdatedBy         uuid.UUID          `json:"updated_by"`
	DeletedAt         pgtype.Timestamptz `json:"deleted_at"`
}

// Thông báo — in-app và email. Worker xử lý gửi email bất đồng bộ
type Notification struct {
	ID          uuid.UUID `json:"id"`
	RecipientID uuid.UUID `json:"recipient_id"`
	// Kênh: IN_APP (hiển thị trên web) hoặc EMAIL
	Channel string `json:"channel"`
	// Loại sự kiện: DEAL_PENDING_APPROVAL, DEAL_REJECTED, DEAL_CANCELLED...
	EventType  string             `json:"event_type"`
	Title      string             `json:"title"`
	Body       string             `json:"body"`
	DealModule *string            `json:"deal_module"`
	DealID     pgtype.UUID        `json:"deal_id"`
	IsRead     bool               `json:"is_read"`
	ReadAt     pgtype.Timestamptz `json:"read_at"`
	// Trạng thái gửi: PENDING → SENT / FAILED / RETRYING
	DeliveryStatus string `json:"delivery_status"`
	// Số lần retry — giới hạn tối đa trong application
	RetryCount int16   `json:"retry_count"`
	LastError  *string `json:"last_error"`
	// Thời điểm retry tiếp theo — exponential backoff
	NextRetryAt pgtype.Timestamptz `json:"next_retry_at"`
	SentAt      pgtype.Timestamptz `json:"sent_at"`
	CreatedAt   time.Time          `json:"created_at"`
}

// Quyền hạn chi tiết — mô hình resource + action cho RBAC
type Permission struct {
	ID uuid.UUID `json:"id"`
	// Mã quyền: FX_DEAL.CREATE, BOND_DEAL.APPROVE_L1...
	Code string `json:"code"`
	// Tài nguyên: FX_DEAL, BOND_DEAL, MM_INTERBANK_DEAL...
	Resource string `json:"resource"`
	// Hành động: VIEW, CREATE, APPROVE_L1, BOOK_L1...
	Action      string    `json:"action"`
	Description *string   `json:"description"`
	CreatedAt   time.Time `json:"created_at"`
}

// Vai trò hệ thống — 10 vai trò theo BRD v3 section 2.4
type Role struct {
	ID uuid.UUID `json:"id"`
	// Mã vai trò: DEALER, DESK_HEAD, CENTER_DIRECTOR, DIVISION_HEAD, RISK_OFFICER, RISK_HEAD, ACCOUNTANT, CHIEF_ACCOUNTANT, SETTLEMENT_OFFICER, ADMIN
	Code        string  `json:"code"`
	Name        string  `json:"name"`
	Description *string `json:"description"`
	// Phạm vi dữ liệu: ALL = toàn bộ, MODULE_SPECIFIC = theo module, STEP_SPECIFIC = theo bước duyệt
	Scope     string    `json:"scope"`
	CreatedAt time.Time `json:"created_at"`
}

// Bảng gán quyền cho vai trò — many-to-many giữa roles và permissions
type RolePermission struct {
	ID           uuid.UUID   `json:"id"`
	RoleID       uuid.UUID   `json:"role_id"`
	PermissionID uuid.UUID   `json:"permission_id"`
	CreatedAt    time.Time   `json:"created_at"`
	CreatedBy    pgtype.UUID `json:"created_by"`
}

// Chỉ dẫn thanh toán (SSI/Pay Code) — mỗi đối tác có thể có nhiều SSI cho cùng tiền tệ
type SettlementInstruction struct {
	ID             uuid.UUID `json:"id"`
	CounterpartyID uuid.UUID `json:"counterparty_id"`
	CurrencyCode   string    `json:"currency_code"`
	// Loại: INTERNAL (KLB) hoặc COUNTERPARTY (đối tác)
	OwnerType string `json:"owner_type"`
	// Số tài khoản thanh toán
	AccountNumber string `json:"account_number"`
	BankName      string `json:"bank_name"`
	// SWIFT code ngân hàng đại lý
	SwiftCode *string `json:"swift_code"`
	// Mã Citad (thanh toán nội địa)
	CitadCode   *string `json:"citad_code"`
	Description *string `json:"description"`
	// SSI mặc định cho counterparty + currency
	IsDefault bool        `json:"is_default"`
	IsActive  bool        `json:"is_active"`
	CreatedAt time.Time   `json:"created_at"`
	CreatedBy pgtype.UUID `json:"created_by"`
	UpdatedAt time.Time   `json:"updated_at"`
	UpdatedBy pgtype.UUID `json:"updated_by"`
}

// Quy tắc state machine — config-driven, thay đổi flow bằng data không cần code
type StatusTransitionRule struct {
	ID uuid.UUID `json:"id"`
	// Module: FX, BOND, MM_INTERBANK, MM_OMO_REPO
	DealModule string `json:"deal_module"`
	// Trạng thái hiện tại
	FromStatus string `json:"from_status"`
	// Trạng thái đích
	ToStatus string `json:"to_status"`
	// Vai trò yêu cầu (FK logic tới roles.code)
	RequiredRole string `json:"required_role"`
	// Bắt buộc nhập lý do (reject, recall, cancel)
	RequiresReason bool `json:"requires_reason"`
	// Hiển thị popup xác nhận trước khi thực hiện
	RequiresConfirmation bool `json:"requires_confirmation"`
	IsActive             bool `json:"is_active"`
}

// Người dùng hệ thống Treasury — hỗ trợ cả standalone auth và Zitadel OIDC
type User struct {
	// UUID v7 — ứng dụng tạo, gen_random_uuid() là fallback
	ID uuid.UUID `json:"id"`
	// ID từ Zitadel IdP khi AUTH_MODE=zitadel
	ExternalID *string `json:"external_id"`
	// Tên đăng nhập — duy nhất trong hệ thống
	Username string `json:"username"`
	// BCrypt hash — chỉ dùng khi AUTH_MODE=standalone
	PasswordHash *string `json:"password_hash"`
	FullName     string  `json:"full_name"`
	Email        string  `json:"email"`
	// Chi nhánh/phòng giao dịch — FK tới branches
	BranchID pgtype.UUID `json:"branch_id"`
	// Phòng ban (K.NV, P.KTTC, BP.TTQT, QLRR...)
	Department *string `json:"department"`
	Position   *string `json:"position"`
	// Trạng thái hoạt động — false = bị vô hiệu hóa
	IsActive    bool               `json:"is_active"`
	LastLoginAt pgtype.Timestamptz `json:"last_login_at"`
	CreatedAt   time.Time          `json:"created_at"`
	UpdatedAt   time.Time          `json:"updated_at"`
	// Soft delete — không bao giờ xóa cứng
	DeletedAt pgtype.Timestamptz `json:"deleted_at"`
}

// Gán vai trò cho người dùng — mỗi user có thể có nhiều vai trò
type UserRole struct {
	ID        uuid.UUID   `json:"id"`
	UserID    uuid.UUID   `json:"user_id"`
	RoleID    uuid.UUID   `json:"role_id"`
	GrantedAt time.Time   `json:"granted_at"`
	GrantedBy pgtype.UUID `json:"granted_by"`
}

// Phiên đăng nhập — chỉ dùng khi AUTH_MODE=standalone, Zitadel dùng IdP session
type UserSession struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
	// SHA-256 hash của session/refresh token — KHÔNG lưu token gốc
	TokenHash string    `json:"token_hash"`
	IpAddress string    `json:"ip_address"`
	UserAgent *string   `json:"user_agent"`
	ExpiresAt time.Time `json:"expires_at"`
	// Thời điểm thu hồi phiên (logout/force-expire)
	RevokedAt pgtype.Timestamptz `json:"revoked_at"`
	CreatedAt time.Time          `json:"created_at"`
}
